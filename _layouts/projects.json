{% assign titles = site.projects | map: "title" |  %}

{
  "posts": [
    {% for project in site.projects %}
     "{{ project.title }}"{% if project != site.projects.last %},{% endif %}
    {% endfor %}
  ]
}
